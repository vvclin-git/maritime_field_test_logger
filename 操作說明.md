# Sea Trial Logger v1 操作說明

## 開始使用

### 本機啟動

- Windows 可直接雙擊專案根目錄的 `start_app.bat`；它會啟動本機 Vite 伺服器並開啟瀏覽器。
- 預設網址為 `http://127.0.0.1:5173/`。第一次執行若尚未安裝依賴，入口會先使用 pnpm 或 npm 安裝。
- 也可以在專案目錄執行 `pnpm dev`，再自行開啟上述網址。

1. 以 HTTPS 網址開啟 App；頁首顯示「可離線使用」後，即可在飛航模式重開。
2. 手機可從 Safari／Chrome 的分享或選單選擇「加入主畫面」。
3. 建立航次並填寫日期、地點、版本、人員與速度；各條件預設 2 次，設為 0 代表不安排。

## 執行 Run

1. 從桌面矩陣或手機情境卡選擇 P1/P2 與 A/B。
2. 核對航跡示意圖、速度與距離，選擇光照並勾選船長確認。
3. 按 START 開始；切換 App 或鎖屏後時間仍依開始時間計算。
4. 按 END，選擇 Completed Yes/No、事件標籤並填寫備註／檔名，再按 SAVE。
5. 儲存後可再跑同條件、切換 A/B 或回進度表。

## 備份與移轉

- 航次資料只存在目前裝置的 IndexedDB；請定期按「備份」。
- 備份會下載完整 JSON 與逐 Run CSV。
- 在另一台裝置選擇「還原 JSON」；同 ID 航次會先顯示差異數量並要求確認，不會自動合併。

## 圖片操作

- 使用 ＋／−、拖曳與重設調整航跡圖；可進入滿版檢視。手機可自由橫放，不會強制方向。
- 圖片只作航跡核對，不代表 GPS、AIS 或即時定位。

## 安全提醒

- 頁首「已儲存至此裝置」只在本機資料庫寫入成功後出現。
- 頁面意外關閉後會恢復進行中 Run 或未完成的結果；系統不會推測 END 時間。
- 更新網站前，先完成或保存正在執行的 Run，並建立 JSON 備份。

## GitHub Pages 部署

- 推送到 `main` 分支後，`.github/workflows/deploy-pages.yml` 會自動建置並部署 `dist`。
- 第一次使用時，請在 GitHub repository 的 **Settings → Pages → Build and deployment**，將 Source 設為 **GitHub Actions**。
- 網站網址為 `https://vvclin-git.github.io/maritime_field_test_logger/`。
- Vite、Manifest、Service Worker 與航跡圖都已使用可在 repository 子路徑運作的相對網址。


## 離線準備與驗收

- 出海前在實際使用的裝置與瀏覽器開啟 GitHub Pages HTTPS 網址，等待「離線下載」完成並顯示「可離線使用」。這表示啟動檔案與 14 張航跡圖均已保存。
- 若顯示「離線未就緒」，保持網路連線並點擊該按鈕重試。桌面滑鼠停留可查看錯誤原因。未完成前不要把這個版本視為可離線使用。
- 出海前切換飛航模式，重新整理或關閉分頁再開啟同一網址，確認航跡圖、START／END、結果儲存與歷史紀錄都可操作。
- 離線可以使用現有環境資料及手動編輯；取得新的 Windy 資料仍需要網路。
- 開發模式（pnpm dev／目前的 start_app.bat）不提供完整離線快取；正式離線版本必須執行 pnpm build，部署整個 dist，包含建置生成的 sw.js。
- 不要清除網站資料或使用無痕模式保存正式測試紀錄；仍請定期匯出 JSON 備份。
