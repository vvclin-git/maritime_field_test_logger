# 海測紀錄 App 實作規格書

版本：1.0  
日期：2026-09-16  
狀態：規畫文件，尚未實作

## 1. 目的與範圍

將船速組合由 P1、P2 擴充為 P1～P4，並新增「重置 App／開始新任務」。本文件中的「任務」指 App 內的航次。

重置必須保留已輸入的 Windy API Key、緯度與經度；不得要求使用者重新輸入。測試情境維持 S01～S07，各情境維持 A/B 兩種航跡。

## 2. 四組船速與測試計畫

### 2.1 船速設定

- 固定顯示 P1、P2、P3、P4，依序排列。
- 每組包含本船與他船速度，單位 kt，允許 0 與一位小數，禁止負值、空白視為 0、非有限數值。
- 四組實際船速尚未由客戶提供；新航次欄位留白，由使用者填寫後儲存，不自行推定速度組合。
- S01 仍以靜止浮標為目標：準備頁顯示「靜止浮標」，Run 快照的目標速度固定為 0；不改寫該組在其他情境使用的他船速度。
- 已有 Run 的航次，既有已設定組別的船速定義維持鎖定。舊航次缺少的 P3、P4 可初次設定；儲存後適用相同鎖定規則。

### 2.2 計畫次數

- 每個情境共 8 個條件：P1-A、P1-B、P2-A、P2-B、P3-A、P3-B、P4-A、P4-B。
- 7 個情境共 56 個條件；新航次預設每條件 2 次，合計 112 次。
- 每情境的「統一次數」套用到該情境全部 8 個條件，可展開個別調整。
- 次數必須為 0～99 的整數；0 表示停用該條件，不能開始測試。
- 個別次數不一致時，統一次數欄位顯示空白及提示，不任意覆寫原值。
- 完成次數只計入 completed=true 的 Run；嘗試次數計入該條件的所有 Run。

### 2.3 畫面與操作

- 進度摘要顯示四組本船／他船速度；未設定組別顯示「待設定」。
- 桌面進度表包含情境欄及 8 個條件欄；窄螢幕使用卡片或可操作的捲動布局，避免按鈕與文字被截斷。
- 手機情境卡片顯示全部 8 個條件。
- 準備、開始、結束、結果、重測、A/B 切換、歷史紀錄皆支援 P1～P4。
- 未設定速度或計畫次數為 0 的條件不得開始 Run；程式入口與畫面均需檢查。
- 保留既有環境設定、光照選擇及與船長確認等開始條件。

## 3. 舊航次與備份相容性

- 讀取與匯入只有 P1、P2 的航次時，保留原船速、計畫次數、Run、時間與環境快照。
- 缺少的 P3、P4 速度設為「未設定」，所有相關計畫次數補為 0，舊航次計畫總數不變。
- 使用者可在航次設定補齊 P3、P4，並設定所需次數後啟用。
- 遷移必須可重複執行，不能覆寫已存在的 P3、P4 或重複建立 Run。
- JSON 備份完整保存四組設定、計畫、Run 與環境版本；CSV phase 欄位支援 P1～P4。
- 建議新 JSON 輸出 schemaVersion=2；匯入支援版本 1、2，版本 1 經遷移再保存。舊版 App 不保證可讀取版本 2。
- 匯入需驗證組別、數值與關聯；驗證失败時不得部分覆寫現有資料。
- Windy API Key 不納入一般航次 JSON／CSV 備份。

## 4. 重置 App／開始新任務

### 4.1 資料範圍

| 資料 | 重置行為 |
| --- | --- |
| 此 App 在目前網站來源、瀏覽器中的全部航次 | 清除 |
| 船速、計畫次數與航次人員／設備等設定 | 隨航次清除 |
| 全部 Run、結果、備註、檔名參照及環境快照 | 清除 |
| 全部航次環境版本與已取得的 Windy 預報紀錄 | 清除 |
| 上次儲存／備份時間、目前選取與操作狀態 | 清除或重設 |
| 已輸入的 Windy API Key | 保留原值 |
| 已輸入的緯度與經度 | 保留原值 |
| App 離線快取、Service Worker、航跡圖 | 保留 |
| 已下載的 JSON／CSV 與外部影音、ECU 檔案 | 不受影響 |

「Windy 資料保留」在本需求中指連線設定（Key、經緯度）；舊航次預報及環境紀錄會清除，新航次需重新取得或填寫環境資料。重置只作用於目前網站來源及瀏覽器儲存空間。

### 4.2 操作流程

1. 使用者在「航次設定」選擇「重置 App／開始新任務」。
2. 若任一分頁仍有計時中的 Run，阻止重置並提示先結束測試。
3. 顯示清除範圍、保留項目及先備份提示，提供「下載備份」、「取消」、「繼續重置」。備份為選用；不將取消分享視為備份成功。
4. 選擇繼續後顯示第二次確認，按鈕標示「確認清除並開始新任務」。任一次取消都不得修改資料。
5. 執行期間停用重複提交與其他寫入，完成清除後回到新航次建立畫面。
6. 新航次取得新 ID，使用 P1～P4；不沿用舊航次的計畫與環境版本。開啟環境設定時，Windy Key、經緯度仍存在。

建議確認文案：

> 即將清除此裝置在本網站保存的所有航次、測試紀錄與環境紀錄。Windy API Key、經緯度與離線功能會保留。已下載的備份檔不受影響。建議先下載備份；清除後需透過備份還原。

### 4.3 一致性與失敗處理

- 重置前再次檢查持久化的 Run 狀態，避免另一分頁剛開始測試。
- 各分頁寫入與重置必須共用互斥機制；單靠 BroadcastChannel 通知不足以防止競態。
- 目前航次與環境位於不同 IndexedDB，不能假設跨資料庫具有單一交易原子性。需以持久化重置標記與可重入步驟支援失敗或關閉後續作。
- 任一步清除失敗，不顯示成功、不允許直接建立新航次；提供重試。下次啟動先偵測未完成重置並恢復處理。
- 重置完成通知其他分頁，讓它們清空舊畫面及記憶體狀態；禁止舊分頁把已刪除航次重新寫回。
- Windy 設定從頭到尾不列入刪除操作；不得用先全刪再還原的方式保留。

## 5. 實作對應與資料模型

現況依本專案原始碼確認：

| 位置 | 現況／預計修改 |
| --- | --- |
| src/main.ts | Phase、Voyage、矩陣、卡片、設定、Run 流程、備份、匯入及工具入口支援四組；新增重置流程 |
| src/environment.ts | Windy 設定獨立保存；提供僅清除環境紀錄的介面 |
| src/style.css | 調整 8 條件顯示及設定表單布局 |
| 操作說明.md | 更新四組船速、舊資料啟用方式及重置說明 |

建議使用單一 PHASES 常數驅動所有組別迴圈，避免各頁自行列舉造成遺漏：

```ts
const PHASES = ['P1', 'P2', 'P3', 'P4'] as const;
type Phase = typeof PHASES[number];
type SpeedPair = [number, number]; // 本船、他船，單位 kt
type Speeds = Record<Phase, SpeedPair | null>; // null：舊航次尚未設定
```

計畫 key 延續既有「情境-A/B-P組別」格式，例如 1-A-P3。進度計算對缺少計畫採 0，不沿用目前預設 2 的 fallback。模型工具 open_run_preparation 的 phase enum 及執行期驗證同步擴充。

儲存清除需採明確白名單：

- IndexedDB sea-trial-logger：清除 voyages、runs；meta 僅清除航次相關資料，重置標記須保留至完成。
- IndexedDB sea-trial-environments：清除 environments。
- localStorage：清除 lastSaved、lastBackup 等明確航次相關 key。
- localStorage 的 sea-trial-windy-settings 必須保持原值；其中包含 key、lat、lon。
- 禁止呼叫 localStorage.clear()、全面清除網站資料，或移除離線快取以實現本功能。

## 6. 驗收條件

| 編號 | 情境與預期結果 |
| --- | --- |
| A01 | 新航次可輸入並保存四組速度；每情境 8 條件、合計 56 條件；預設合計 112 次 |
| A02 | 統一次數更新全部 8 條件；個別調整、0 次停用與總數計算一致 |
| A03 | P3、P4 可完成準備→開始→結束→結果→歷史流程；快照速度與所選組別一致 |
| A04 | S01 在四組中目標皆顯示靜止浮標，快照目標速度皆為 0 |
| A05 | 舊航次載入或版本 1 備份匯入後，原有資料及計畫總數不變；P3、P4 待設定且計畫為 0 |
| A06 | 已有 Run 的舊航次可補齊 P3、P4，不能修改已鎖定的原組別速度 |
| A07 | 四組 JSON 匯出再匯入資料一致；CSV 正確包含 P3、P4；一般備份不含 API Key |
| A08 | 兩階段重置任一步取消後，持久化與畫面資料不變 |
| A09 | 重置前後 sea-trial-windy-settings 原值一致；重新整理與建立新航次後 Key、lat、lon 仍可沿用 |
| A10 | 重置後所有舊航次、Run、環境紀錄及舊進度均消失；新航次為新 ID |
| A11 | 本分頁或另一分頁正在計時時，無法重置；重置與開始同時操作時不出現半清除 Run |
| A12 | 模擬其中一個資料庫清除失敗或途中關閉，重新啟動可恢復重置；不誤報成功、不丟失 Windy 設定 |
| A13 | 其他已開分頁不顯示可操作的舊航次，也無法寫回舊資料 |
| A14 | 離線狀態可重置、建立新航次並查看航跡；Windy 更新仍需要網路 |
| A15 | 桌面與手機均能操作全部 8 條件與兩次重置確認，無遮擋或不可達控制項 |

驗證包含型別檢查與正式建置、必要的資料遷移及重置測試，以及瀏覽器端 IndexedDB、跨分頁與離線流程驗證。單純檢查畫面文字不足以證明 Windy 設定保留。

## 7. 待提供資訊與交付

唯一待提供的業務資料為 P1～P4 各自的實際本船／他船速度；這不阻礙先實作可輸入欄位。

本文件中版本 2 備份、資料模型與復原機制屬建議實作設計；功能行為與 Windy 設定保留為本次需求基準。後續實作交付應包含程式修改、更新後操作說明與上述驗收結果。本次僅交付規格書，不執行重置或修改 App 程式。
